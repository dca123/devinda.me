{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NOTION_API_KEY", "CLOUDINARY_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NOTION_API_KEY", "CLOUDINARY_URL"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
