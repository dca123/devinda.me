{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CLOUDINARY_URL", "NOTION_API_KEY"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": []
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
