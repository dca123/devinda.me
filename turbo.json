{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NOTION_API_KEY", "CLOUDINARY_URL"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
