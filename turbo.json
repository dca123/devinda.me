{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["CLOUDINARY_URL", "NOTION_API_KEY"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
